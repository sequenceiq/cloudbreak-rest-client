{
    "cloudPlatform": "$CLOUD_PLATFORM",
    "name": "$NAME",
    "description": "$DESCRIPTION",
    "volumeCount": "$VOLUME_COUNT",
    "volumeSize": "$VOLUME_SIZE",
    "public": $PUBLIC_IN_ACCOUNT,
    "parameters": {
        "vmType": "$INSTANCE_TYPE"
    }
}