{
    "cloudPlatform": "$CLOUD_PLATFORM",
    "name": "$NAME",
    "description": "$DESCRIPTION",
    "parameters": {
        "location": "$REGION",
        "vmType": "$INSTANCE_TYPE",
        "imageName": "$IMAGE_NAME",
        "password": "$PASSWORD",
        "sshPublicKey": "$SSHKEY"
    }
}