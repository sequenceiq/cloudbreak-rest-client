{
   "cloudPlatform":"$CLOUD_PLATFORM",
   "name":"$NAME",
   "description": "$DESCRIPTION",
   "publicKey": "$SSHKEY",
   "publicInAccount": "$PUBLIC_IN_ACCOUNT",
   "parameters":{
      "roleArn":"$ROLE_ARN"
   }
}
