{
   "cloudPlatform":"$CLOUD_PLATFORM",
   "name":"$NAME",
   "description": "$DESCRIPTION",
   "publicKey": "$SSHKEY",
   "public": $PUBLIC_IN_ACCOUNT,
   "parameters":{
      "roleArn":"$ROLE_ARN"
   }
}
