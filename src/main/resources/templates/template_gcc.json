{
    "cloudPlatform": "$CLOUD_PLATFORM",
    "name": "$NAME",
    "description": "$DESCRIPTION",
    "publicInAccount": "$PUBLIC_IN_ACCOUNT",
    "parameters": {
        "region": "$REGION",
        "amiId": "$AMI",
        "sshLocation": "$SSH_LOCATION",
        "instanceType": "$INSTANCE_TYPE"
    }
}