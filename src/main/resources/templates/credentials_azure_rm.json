{
   "cloudPlatform":"$CLOUD_PLATFORM",
   "name":"$NAME",
   "description": "$DESCRIPTION",
   "publicKey": "$SSHKEY",
   "parameters":{
       "subscriptionId": "$SUBSCRIPTIONID",
       "secretKey": "$SECRETKEY",
       "tenantId": "$TENANTID",
       "accesKey": "$ACCESKEY"
   }
}