{
   "cloudPlatform": "$CLOUD_PLATFORM",
   "name": "$NAME",
   "description": "$DESCRIPTION",
   "publicKey": "$SSHKEY",
   "parameters":{
      "user": "$USERNAME",
      "password": "$PASSWORD",
      "tenantName": "$TENANTNAME",
      "endpoint": "$ENDPOINT"
   }
}