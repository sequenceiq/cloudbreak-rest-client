{
    "cloudPlatform": "$CLOUD_PLATFORM",
    "name": "$NAME",
    "description": "$DESCRIPTION",
    "volumeCount": "$VOLUME_COUNT",
    "volumeSize": "$VOLUME_SIZE",
    "parameters": {
        "gcpInstanceType": "$GCP_INSTANCE_TYPE",
        "volumeType": "$GCP_VOLUME_TYPE"
    }
}